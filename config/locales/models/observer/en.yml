en:
  activerecord:
    models:
      observer: observer
    attributes:
      observer:
        user_id: user
        meeting_id: meeting
        created_at: created at
        updated_at: updated at
